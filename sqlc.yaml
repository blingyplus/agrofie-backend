version: "1"

packages:
  - path: db/queries
    name: queries
    engine: postgresql
    schema: db/migrations
    queries: db/queries
    gen:
      go:
        package: "db"
        out: "internal/db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_empty_slices: true
